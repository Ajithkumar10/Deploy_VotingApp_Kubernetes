apiVersion: v1
kind: service
metadata:
  name: postgres
  labels: 
    name: postgres-service
    app: demo-voting-app
spec:
  ports:
  - ports: 5432
    targetPort: 5432
  selector:
    name: postgres-pod
    app: demo-voting-app

   
